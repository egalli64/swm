-- HR SCHEMA

-- 1. EMPLOYEES: highest, lowest, total sum, (rounded) mean of salaries

 
-- 2. EMPLOYEES: same as (1), but grouped by job id
 
 
-- 3. EMPLOYEES: how many have the same job id?
 
 
-- 4. EMPLOYEES: how many managers?
 
 
-- 5. EMPLOYEES: delta between lowest and highest salary


-- 6. EMPLOYEES: for each manager, which is the lowest salary?
--               do not consider 'no manager' and salary over 6000

 